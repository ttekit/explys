{
  "dependencies": {
    "@types/pg": "^8.20.0",
    "pg": "^8.22.0",
    "xlsx": "^0.18.5"
  },
  "scripts": {
    "prepare": "husky",
    "slack:dev": "bash scripts/keep-slack-alive.sh",
    "slack:run": "cd explysslack && slack run",
    "test:record-constellation": "node scripts/record-constellation-feature.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "playwright": "^1.63.0"
  }
}
