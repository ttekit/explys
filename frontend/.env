VITE_API_BASE_URL=https://api.explys.com
VITE_API_BASIC_AUTH_USER=admin
VITE_API_BASIC_AUTH_PASSWORD=thiphoNee4

# Dev: same-origin proxy so the remote API does not need localhost in CORS_ORIGINS
VITE_USE_API_PROXY=1
VITE_DEV_PROXY_TARGET=https://api.explys.com
# Sent as `x-api-token`; must equal backend `API_TOKEN`. Escape `$` as `\$` (Vite expands `$var` in .env).
VITE_API_TOKEN="tests"
VITE_PUBLIC_POSTHOG_KEY="phc_nVbWUzd2AcAogogHmtNvWdiJvwqry2cDzKvD34yNaaPn"
VITE_PUBLIC_POSTHOG_HOST="https://eu.i.posthog.com"
